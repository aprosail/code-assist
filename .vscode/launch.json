{
  "configurations": [
    {
      "name": "Code Assist",
      "args": [
        // Specify folder to open when launch extension.
        "${workspaceFolder}/example",
        "--extensionDevelopmentPath=${workspaceFolder}/code-assist/out"
      ],
      "preLaunchTask": "npm: build - code-assist",
      "request": "launch",
      "type": "extensionHost"
    }
  ]
}
