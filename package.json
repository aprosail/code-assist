{
  "name": "code-assist-workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.5.1",
    "prettier-plugin-sh": "^0.14.0"
  }
}
